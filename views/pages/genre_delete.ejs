<!DOCTYPE html>
<html>
  <head>
    <%- include('../partials/head'); %>
  </head>
  <body>
    <div class="container-fluid">
      <div class="row">
        <%- include('../partials/sidebar_nav'); %>
        <div class="col-sm-10 p-3">
            <h1>Delete genre?</h1>
            <% if (locals.items.length > 0) { %>
            <p><strong>You may not delete the <%- genre.name %> genre until you update or delete every item under the genre.</strong></p>
            <a href="../<%- item._id %>">
              <button class="btn btn-primary" type="button">Go back</button>
            </a>
            <% } else { %>
            <p><strong>Are you sure you want to delete the <%- genre.name %> genre?</strong></p>
            <div class="row">
                <form method="post" action="">
                    <div class="form-group">
                        <input type="hidden" name="id" value="<%- genre._id %>" required>
                        <div class="row">
                          <div class="col-auto">
                            <a href="../<%- genre._id %>">
                              <button class="btn btn-primary" type="button">Go back</button>
                            </a>
                          </div>
                          <div class="col-auto">
                            <a href="<%- genre._id %>/delete">
                              <button class="btn btn-danger" type="submit">Delete</button>
                            </a>
                          </div>
                        </div>
                    </div>
                </form>
            </div>
            <% } %>
        </div>
      </div>
    </div>
  </body>
</html>